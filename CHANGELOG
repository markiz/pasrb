v0.0.5.1 Changes:
* Fixed bug in symbolize_keys on 1.8.6

v0.0.5. Changes:
* #websites api method

v0.0.4. Changes:
* Most operations return array of hashes now.

v0.0.3. Changes:
* get_member_tracker_stats no longer keeps three forms of keys.
  Now it only uses "identifier" => data scheme, because it was
  quite difficult to iterate otherwise.
* Same for all_members


v0.0.2. Changes:
* website_offers api method call

v0.0.1. Initial release